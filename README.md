# SMASYS - School Management Backend

Quick scaffold for an Express + MongoDB backend.

Install:

```
npm install
```

Run (dev):

```
npm run dev
```

Edit `.env` with your `MONGO_URI` and `PORT`.
